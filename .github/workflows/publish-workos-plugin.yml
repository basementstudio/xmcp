name: Publish @xmcp-dev/workos

on:
  workflow_dispatch:

permissions:
  id-token: write
  contents: read

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10.8.1

      - uses: actions/setup-node@v4
        with:
          node-version: "20"
          registry-url: "https://registry.npmjs.org"
          cache: "pnpm"

      - name: Install dependencies
        run: pnpm install --filter @xmcp-dev/workos --frozen-lockfile

      - name: Build @xmcp-dev/workos
        run: pnpm --filter @xmcp-dev/workos build

      - name: Test @xmcp-dev/workos
        run: pnpm --filter @xmcp-dev/workos test

      - name: Publish @xmcp-dev/workos
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
        run: |
          cd packages/plugins/workos
          pnpm publish --access public --no-git-checks
