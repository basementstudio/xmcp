{
  "name": "Template Config",
  "description": "Modify the template config in the xmcp.config.ts file.",
  "keywords": [
    "config"
  ],
  "scripts": {
    "build": "xmcp build",
    "dev": "xmcp dev",
    "start": "node dist/http.js"
  },
  "dependencies": {
    "xmcp": "workspace:*",
    "zod": "3.25.0"
  }
}
