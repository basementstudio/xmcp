# Stripe secret key with machine payments enabled
STRIPE_SECRET_KEY=sk_test_YOUR_STRIPE_SECRET_KEY_HERE

# Optional: Persistent secret for MPP challenges (base64-encoded)
# If not set, an ephemeral key is auto-generated (won't survive server restarts)
# MPP_SECRET_KEY=your_base64_encoded_secret_here
