{
  "name": "scalekit-http-example",
  "private": true,
  "description": "HTTP-based xmcp server with Scalekit authentication",
  "keywords": [
    "http",
    "authentication",
    "scalekit"
  ],
  "scripts": {
    "build": "xmcp build",
    "dev": "xmcp dev",
    "start": "node dist/http.js"
  },
  "dependencies": {
    "@xmcp-dev/scalekit": "workspace:*",
    "xmcp": "workspace:*",
    "zod": "^4.0.10"
  },
  "devDependencies": {
    "@xmcp-dev/compiler": "workspace:*"
  }
}
