---
title: "Transports"
metadataTitle: "HTTP Transport Configuration | xmcp Documentation"
publishedAt: "2025-07-06"
summary: "Learn how to customize the HTTP transport configuration."
---

The `http` configuration can be used to configure the HTTP server.

```typescript
const config: XmcpConfig = {
  http: {
    port: 3000,
    // The endpoint where the MCP server will be available
    endpoint: "/my-custom-endpoint",
    bodySizeLimit: 10 * 1024 * 1024,
    cors: {
      origin: "*",
      methods: ["GET", "POST"],
      allowedHeaders: ["Content-Type"],
      credentials: true,
      exposedHeaders: ["Content-Type"],
      maxAge: 600,
    },
  },
};

export default config;
```
